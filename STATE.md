# STATE

- current goal: keep the Alchemy site bundle isolated as its own Codex project repo
- current task: preserve the local HTML entrypoints and browser-open helpers without shared parent-repo context
- next step: connect the dedicated GitHub remote and enable the workspace preservation flow
